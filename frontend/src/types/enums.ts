export enum Color {
    WHITE = 10,
    BLUE = 20,
    BLACK = 30,
    RED = 40,
    GREEN = 50,
}

export enum GameFormat {
    LEGACY = 60,
    STANDART = 61,
    MODERN = 69,
    COMMANDER = 100,
}
